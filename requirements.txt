# Core application dependencies
Flask==2.3.3
Flask-CORS==4.0.0
requests==2.31.0
python-dotenv==1.0.1
PyJWT==2.8.0
Werkzeug==2.3.7

# Dependencies for AI Agent
PyAutoGUI==0.9.54
Pillow==10.1.0

# Optional: For deployment (uncomment and adjust versions as needed)
# gunicorn>=20.1.0
# waitress>=2.1.0

# Optional: For advanced security (uncomment if used, ensure compatibility)
# cryptography>=3.4.8
# bcrypt>=3.2.0

# Optional: For monitoring (uncomment if used)
# psutil>=5.9.0
