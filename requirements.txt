# متطلبات النظام للنشر
requests>=2.28.0
sqlite3  # مدمج في Python
hashlib  # مدمج في Python
secrets  # مدمج في Python

# للنشر على خوادم الإنتاج (اختياري)
gunicorn>=20.1.0
flask>=2.3.0
waitress>=2.1.0

# للأمان المتقدم (اختياري)
cryptography>=3.4.8
bcrypt>=3.2.0

# للمراقبة والتسجيل (اختياري)
psutil>=5.9.0
