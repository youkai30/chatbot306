# 🚀 دليل التشغيل السريع - نظام الشات بوت الذكي المتكامل

## 📋 نظرة عامة سريعة
هذا الدليل يساعدك على تشغيل نظام الشات بوت الذكي بسرعة، والذي يتضمن الآن خادمًا خلفيًا موحدًا (`real_backend.py`) يخدم واجهة الشات بوت وواجهات برمجة التطبيقات (APIs)، بالإضافة إلى وكيل ذكي (AI Agent) اختياري.

## ⚡ التشغيل السريع (للنظام الأساسي)

### الخطوة 1: إعداد البيئة والمتطلبات (إذا لم تقم بذلك من `README.md`)
1.  **تأكد من تثبيت Python 3.8+**.
2.  **إذا لم تكن قد استنسخت المستودع، قم بذلك الآن.**
3.  **قم بإنشاء ملف `.env`** في المجلد الجذر للمشروع وأضف إليه مفتاح DeepSeek API الخاص بك (والمفاتيح السرية الأخرى كما هو موضح في `README.md` الرئيسي):
    ```env
    DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    SECRET_KEY=your_flask_secret_key_here
    JWT_SECRET_KEY=your_jwt_secret_key_here
    # BACKEND_PORT=8001 (اختياري)
    ```
4.  **تثبيت المتطلبات (من المجلد الجذر للمشروع)**:
    ```bash
    pip install -r requirements.txt
    ```
5.  **إعداد قاعدة البيانات (لأول مرة):**
    ```bash
    python setup_database.py
    ```

### الخطوة 2: تشغيل الخادم الخلفي الموحد
من المجلد الجذر للمشروع، قم بتشغيل:
```bash
python real_backend.py
```
أو استخدم أحد ملفات `.bat` المخصصة إذا كنت على نظام Windows (مثل `START_REAL_SYSTEM.bat` أو `START_SECURE_SYSTEM.bat`).

يفترض أن يعمل الخادم الآن على `http://localhost:8001` (أو المنفذ الذي حددته في `.env`).

### الخطوة 3: تجربة الشات بوت
1.  **افتح المتصفح** واذهب إلى واجهة الشات بوت، والتي يتم خدمتها الآن بواسطة `real_backend.py`:
    `http://localhost:8001/web-integration/chatbot-widget.html` (استبدل `8001` بالمنفذ الصحيح إذا تم تغييره).
2.  **انقر على أيقونة الشات بوت** (عادة في الزاوية).
3.  **جرب إرسال رسائل**:
    *   `مرحبا`
    *   `ما هو الوقت؟`
    *   إذا قمت بإعداد مفتاح DeepSeek API بشكل صحيح، يجب أن تحصل على ردود ذكية.

## 🤖 تشغيل الوكيل الذكي (AI Agent) - اختياري ومتقدم

إذا كنت ترغب في استخدام ميزات التحكم في النظام عبر الوكيل الذكي:

### الخطوة 1: تأكد من تثبيت المتطلبات
المتطلبات الأساسية للوكيل (`PyAutoGUI`, `Pillow`, `Flask`, `requests`) يجب أن تكون قد ثبتت مع `pip install -r requirements.txt` من الخطوة الأولى.

### الخطوة 2: تشغيل الوكيل الذكي
1.  افتح نافذة طرفية جديدة.
2.  انتقل إلى مجلد الوكيل الذكي:
    ```bash
    cd ai-agent
    ```
3.  قم بتشغيل الوكيل:
    ```bash
    python simple_run.py
    ```
    سيعمل الوكيل عادة على `http://localhost:5000`.

### الخطوة 3: تجربة أوامر الوكيل عبر الشات بوت
بعد تشغيل كل من `real_backend.py` و `ai-agent/simple_run.py`:
1.  من واجهة الشات بوت (`http://localhost:8001/web-integration/chatbot-widget.html`).
2.  جرب إرسال أوامر يفترض أن يعالجها AI Agent (إذا كان الشات بوت مبرمجًا لتمريرها):
    *   `افتح المفكرة`
    *   `التقط لقطة شاشة`
    (ملاحظة: قد يتطلب هذا أن يكون `real_backend.py` مهيأً لإعادة توجيه هذه الأوامر إلى API الخاص بالوكيل الذكي.)

## 🎯 الميزات الرئيسية (بعد التحديثات)
*   **خادم خلفي موحد**: `real_backend.py` يخدم كل من الـ API والواجهة الأمامية للشات بوت ولوحة التحكم.
*   **إعداد مركزي**: استخدام ملف `.env` للإعدادات الحساسة مثل مفتاح DeepSeek API.
*   **تثبيت متطلبات مبسط**: عبر ملف `requirements.txt` جذري.
*   **شات بوت ذكي**: مع DeepSeek AI (يتطلب مفتاح API).
*   **وكيل ذكي (AI Agent)**: للتحكم في مهام النظام (يعمل بشكل منفصل).

## 🔍 استكشاف الأخطاء الشائعة
*   **مشكلة: الشات بوت لا يرد أو يعطي أخطاء متعلقة بـ API.**
    *   **الحل**: تأكد من أن `real_backend.py` يعمل بدون أخطاء. تحقق من أنك قمت بإنشاء ملف `.env` ووضعت فيه `DEEPSEEK_API_KEY` صحيحًا. تأكد من عدم وجود أخطاء في نافذة الطرفية التي تشغل `real_backend.py`.
*   **مشكلة: الوكيل الذكي (AI Agent) لا يستجيب للأوامر.**
    *   **الحل**: تأكد من أن `ai-agent/simple_run.py` يعمل في نافذة طرفية منفصلة بدون أخطاء. تأكد من أن الشات بوت (المنطق في `real_backend.py` أو JavaScript الخاص بالشات بوت) مهيأ لإرسال الأوامر المناسبة إلى API الوكيل الذكي (عادة على `http://localhost:5000`).
*   **مشكلة: خطأ "Flask not found" أو مكتبات أخرى مفقودة.**
    *   **الحل**: تأكد من أنك قمت بتشغيل `pip install -r requirements.txt` في المجلد الجذر للمشروع وضمن البيئة الافتراضية الصحيحة (إذا كنت تستخدم واحدة).

## 💡 نصائح مهمة
1.  **ابدأ بتشغيل `real_backend.py`** وتأكد من عمل الشات بوت الأساسي قبل محاولة تشغيل AI Agent.
2.  **راجع ملف `.env`** وتأكد من صحة مفتاح DeepSeek API.
3.  **استخدم ملفات التشغيل `.bat`** على Windows (مثل `START_FULL_SYSTEM.bat`) لتسهيل عملية بدء تشغيل المكونات المتعددة.

**🚀 استمتع باستخدام نظام الشات بوت الذكي المحدث!**
